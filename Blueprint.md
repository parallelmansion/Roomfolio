https://github.com/parallelmansion/Roomfolio/blob/main/Blueprint.md

# Roomfolio Blueprint


## このドキュメントについて

Blueprint.md は Roomfolio の唯一の仕様書です。

サービス仕様は Blueprint.md を正とします。

仕様変更は必ず Blueprint.md を起点として行い、README.md・VISION.md・ROADMAP.md・CLAUDE.md は Blueprint.md に合わせて更新してください。

各項目は次の3つに分類されます。

- 決定事項
- 検討中
- 将来構想

AIは「決定事項」のみを実装対象としてください。

「検討中」「将来構想」は独自判断で実装せず、提案として扱ってください。


## サービス概要

Roomfolioは、作者のプロフィールを「部屋」として作成・公開できるサービスである。

訪問者は部屋を訪れ、その雰囲気を通して作者と出会い、作品や活動へ自然につながることができる。


## サービス全体

Roomfolioは次の要素で構成される。

アカウント
│
├ プロフィール
└ 部屋
    ├ 背景
    ├ アセット
    └ リンク


## ユーザー体験

Roomfolioには、2つの体験がある。

### 作者

ポートフォリオとして、自分だけの部屋を作る体験。

背景やアセットを配置し、作品やSNSなどの活動への入口を家具として表現する。

### 訪問者

作者と出会う体験。

検索やおすすめから作者を見つけ、部屋を訪れる。

部屋の雰囲気から作者を知り、家具を通して作品や活動へ触れる。

気に入った作者はフォローやブックマークを行い、継続して訪れることができる。

---

## デザイン方針

Roomfolioの世界観を維持しながら、
現代的で使いやすいUIを目指す。

デザインの細部（レイアウト・余白・アニメーション・配色・操作感）は、
Blueprintの仕様を満たす範囲で最適な提案を採用する。


### デザイン原則

- ユーザーの部屋を主役にする
- サービス自体はできるだけシンプルにする
- UIは作品や部屋を邪魔しない
- スマートフォンを基準に設計する
- PCでは部屋を大きく、美しく見せることを優先する
- サイトフレームは必要最小限とする


---


## アカウント

### 概要

Roomfolioを利用するためのアカウント情報。

1つのアカウントにつき、1つの部屋を所有する。

### 決定事項

#### ユーザー登録

- 表示名
- ユーザーID
- パスワード

#### ログイン

既存ユーザーは、Emailとパスワードを使用してログインする。

**ログインページ**

```text
/login
```

ログインに成功した場合は、作者用Homeへ進む。


#### 管理機能へのアクセス

公開ページは、ログインしなくても閲覧できる。

公開ページを閲覧しているユーザーを、自動的にログインページへ遷移させない。

編集や管理機能へ進む操作を行った場合に、認証を要求する。

ログイン中のAuth UIDに対応するRoomがない場合は、Roomを自動で作成しない。

Roomが見つからない場合は、操作を停止して案内を表示する。


#### ユーザーID

**形式**

```text
room001_yoimitsu
```


#### ユーザーIDの役割

ユーザーIDは、Roomfolio上でユーザーを識別する公開用IDとして使用する。

- 他のユーザーから検索できる
- 公開RoomのURLに使用する
- ユーザーが他者へ伝えて、Roomfolioの自分の部屋を訪れてもらうために使用する

**公開URL**

公開Roomは、ユーザーIDをURLのパスとして使用する。

```text
/{ユーザーID}

例：
/room001_yoimitsu
```

公開Room URLの基準URLは、Stage 3 : Public Roomの実装前に決定する。

#### ユーザーIDと認証ID

RoomfolioのユーザーIDと、認証システムが発行するIDは別のものとして扱う。

- RoomfolioユーザーID
  - 例：`room001_yoimitsu`
  - 公開用
  - URL・検索・ユーザー識別に使用する

- Supabase Auth UID
  - UUID
  - 内部用
  - ログインユーザーの識別・所有者確認に使用する
  - 公開URLには使用しない

RoomfolioユーザーIDからSupabase Auth UIDを直接公開することはしない。



**ルール**

- ユーザーIDは「部屋番号 + 名前」の組み合わせで構成する
- 部屋番号はユーザーが001〜999の中から選択する
- 同じ組み合わせは登録できない
- 同じ名前でも部屋番号が異なれば登録できる
- 同じ部屋番号でも名前が異なれば登録できる
- 1つのアカウントにつき、1つの部屋を所有する

**例**

```text
room001_yoimitsu
room482_yoimitsu
room777_yoimitsu
```

※それぞれ独立したアカウント、別アカウントとして登録する。



### 検討中

- なし

### 将来構想

- なし



---

## プロフィール

### 概要

ユーザー名をタップすると表示される補足ページ。

部屋だけでは伝えきれない情報をまとめる。

### 決定事項

#### 表示内容

- プロフィール
- 各リンクの説明
- リンク一覧
- 更新情報


#### 表示方法

- プロフィールはポップアップで表示する
- ホーム画面からプロフィールを開ける
- ポップアップの具体的な配置・デザインはホーム画面設計時に決定する

#### 更新情報

- 日付 + メモを記録できる
- 必要に応じて表示タイトルを変更できる
  - 例：更新情報 → つぶやき
- 必要に応じて項目を追加できる
  - 例：更新情報・つぶやき・メモなど複数並べることが可能

#### 公開設定

- フォロー一覧・フォロワー一覧は表示できる
- ユーザーごとに公開・非公開を設定できる


#### 初期状態

新しく登録した部屋は次の状態で作成される。

- 背景：デフォルト背景（Default.webp）
- アセット：なし
- プロフィール：未入力

Default.webpは初期背景で、選択肢には含めない。


### 検討中

- プロフィール表示ボタンの置く位置
- UI
- デザイン

### 将来構想

- 活動年表
- 更新履歴の自動生成



---

## 部屋

### 概要

ユーザーは背景画像とアセットを組み合わせ、自分だけの部屋をプロフィールとして公開できる。

#### 公開Roomへのアクセス

公開されたRoomは、ユーザーIDを使用してアクセスできる。

```text
/{ユーザーID}

例：
/room001_yoimitsu
```

公開Room URLは、現在のVercel URLを基準URLとして使用する。

将来的に独自ドメインやアプリ化へ移行する可能性はあるが、現時点では移行しない。


### 決定事項


#### 公開状態

- Roomの保存と公開状態は別に扱う。
- 非公開のRoomでも、作者は自由に背景・アセット・リンク・テーマなどを編集・保存できる。
- Roomを公開するかどうかは作者が選択できる。
- 公開しないRoomを作成・保持することもできる。
- 公開・非公開の切り替えは作者用Homeから行う。


#### 非公開Room

- 非公開Roomは、公開Roomとは異なる公開状態として管理する。
- 非公開Roomでも作者はRoomを編集・保存できる。
- 非公開Roomの予定公開URLは作者が確認できる。
- 非公開RoomのURLは、作者が他者へ共有できる。
- 非公開Roomへアクセスした訪問者には、Roomの家具・プロフィールなどの公開情報を表示しない。
- 非公開Roomと存在しないRoomは、訪問者からは区別できない表示とする。


#### 背景

- プリセットから選択
- 画像アップロード

#### 表示

**スマホ**

- 縦画面表示
- 背景画像の中央を表示

**PC**

- 横画面表示
- 基本は全画面表示

**画像仕様**

- 横画像推奨
- 縦画像は中央表示
- 左右は部屋のテーマの色に合わせて、余白で表示


#### 部屋のテーマ

部屋ごとにテーマを設定できる。

部屋のテーマはプロフィール画面やRoomfolioのUIにも反映される。

反映対象

- フレーム
- ポップアップ
- ボタン
- アクセントカラー

訪問者には、その部屋のテーマが適用される。



### 検討中


### 将来構想

- なし

---

## アセット

### 概要

部屋に配置する家具・装飾・画像。

アセットは装飾だけではなく、コンテンツへの入口として機能する。

### 決定事項

#### 配置数

- 0個～8個の配置が可能。
- 現在の上限は8個
- スマートフォンでの操作性を考慮


#### 種類

- Frame
- Object
- オリジナル画像

#### 表示画像

- フレーム内へアップロードした画像を設定できる
- アップロード画像だけをポスターのように配置できる
- 背景透過画像を利用することでオリジナルアセットとして利用できる
- アップロード画像は外部ストレージへ保存し、Roomfolioでは画像URLを管理する

保存先としてSupabase Storageを採用する。


#### 画像の公開範囲

- 非公開Roomの画像は、画像URLを知っているだけでは直接取得できない状態とする。
- Roomの画像は恒久的な公開URLとして直接配布しない。
- Storageは、非公開Roomの画像を外部から直接取得できない構成を基本とする。
- 画像の閲覧画面をスクリーンショット等で保存することは防止対象としない。
- 右クリック等による簡易的な画像保存は、可能な範囲で防止する。


#### Storage画像の保持

- Roomごとに保持する画像ファイルは最大10個とする。
- Roomで現在使用している画像は、保持数の上限を超えても削除対象としない。
- 保持数が10個を超える場合は、現在使用されていない画像のうち、古いものから削除する。
- 新しい画像の保存が成功した後に、不要になった古い画像を整理する。
- この上限は、1つのRoomで同時に使用できるアセット数が最大8個であり、背景画像を含めて最大9個の画像を使用できることを前提に、予備の1個を確保しながらStorageへの不要な画像の蓄積を抑えるために設定する。


#### Frame

Frame画像表示仕様
Frame画像は**表示領域（窓）**を持ちます。
ユーザー画像は、表示領域内に object-fit: cover 相当で表示してください。
ユーザー画像の縦横比は変更しません。
表示領域からはみ出した部分はクリッピングしてください。
フレーム画像は最前面に表示し、ユーザー画像はフレーム画像の背面に表示してください。

#### アセット配置

- ドラッグ&ドロップは行わない
- 自由配置は行わない
- アセットごとに配置位置を事前に決めておく
- ユーザーは1〜8の配置位置から選択して配置する
- 同じ配置位置に複数のアセットを配置することはできない

#### 配置モード

- 固定配置
  - ユーザーが1〜8の配置位置を選択する

- ランダム配置
  - アセットを自動でランダムな位置に配置する
  - アセット同士は重ならない
  - 画面外へはみ出さない
  - 更新のたびにランダムに配置が変わる

固定配置・ランダム配置のどちらの場合も、アセット同士は重ならない

### 利用制限

#### Stage 3における画像制限

- Stage 3では、新たな画像容量・画像サイズ・画像形式の制限を追加しない。
- 将来的に必要性が生じた場合は、別途検討する。



### 検討中

- アセット数の変更
- 配置位置

### 将来構想

- 家具共有機能
- 家具マーケット

---

## リンク

### 概要

アセットごとに外部サービスへのリンクを設定する。

### 決定事項

- 1つのアセットに複数リンクを設定できる
- リンクを設定しないこともできる
- アセットを押しすることで、説明文を表示させる

#### 設定項目

- URL
- 表示名

#### 表示方法

- リンクはポップアップで表示する
- ポップアップは部屋の中に表示する
- リンクを確認している間も部屋から離れない

#### 閉じる

- ポップアップ外をタップすると閉じる
- 右上に「×」ボタンを表示する


### リンク表示

- 1つのアセットに複数リンクを設定できる
- リンクの表示方法をアセットごとに選択できる
  - 横並び
  - 縦並び
- どちらも中央揃えとする
- 横並びの場合、表示領域に収まらないリンクは項目単位で次の行へ送る
- リンクの登録順を維持する
- アイコン・文字・記号など、リンクの表示内容はユーザーが自由に設定できる

- 外部リンクは新しいタブ等で開き、RoomfolioのRoomを残したまま外部サイトへ移動できるようにする。


### 検討中



### 将来構想

- カテゴリー別表示
- 利用頻度順表示



---

## 検索

### 概要

作者との新しい出会いを生み出す機能。

SNSやWebサイトを個別に検索するのではなく、
作者が設定したプロフィール、更新情報、リンク説明などを横断的に検索できる。

検索結果から部屋を訪れ、作者の活動全体へアクセスできる。

### 決定事項

#### 検索対象

- 表示名
- ユーザーID
- プロフィール
- 更新情報
- 各リンクの説明文

検索はできるだけ広範囲を対象とする。

例

「Disney」

↓

「Disney日記更新中」

という説明文も検索対象に含める。


#### 検索順位

1. 表示名
2. プロフィール
3. リンク説明
4. 更新情報

同じ単語が複数存在する場合

- 更新情報は最新の日付を優先する
- リンク説明は最初の説明を表示する


検索結果には

- ユーザー名
- 該当箇所の前後数文字

を表示する。


#### リンク数

リンク数はできるだけ制限しないことを目指す。

スクロールによって多数のリンクを表示できるようにする。

利用制限は今後検討する。


### 検討中

- 検索結果画面
- 並び順
- おすすめとの組み合わせ

### 将来構想

- タグ検索
- 高度な検索


---


## ホーム

### 概要

ログイン後に最初に表示されるユーザー用ページ。

自分の部屋を確認したり、検索やフォローなど、Roomfolio内の各機能へアクセスする入口となる。

このホームは、ログインした本人が自分のRoomfolioを確認・管理するための作者用Homeである。

作者用Homeと編集画面は別のページとして扱う。

作者は、作者用Homeから編集画面へ進み、Roomの背景、アセット、リンクなどを編集する。


### 決定事項

- 自分の部屋を表示する

- 自分の公開ユーザーIDを確認できる
- 自分の公開RoomへのURLを確認・共有できる

- Roomの公開・非公開を切り替えられる。
- 非公開Roomでも、予定公開URLを確認できる。
- 非公開Roomでも、Room URLをコピー・共有できる。
- 公開Room URLは、コピーや端末の共有機能などを利用して外部サービスへ共有しやすくする。

- 編集画面への入口を用意する

- 部屋をメインとして表示する
- プロフィールへの入口を用意する
- 編集画面への入口を用意する
- その他のボタン配置・ナビゲーションはホーム画面設計時に決定する

### 検討中

- 編集ボタン
- 検索ボタン
- フォローボタン
- フォロワーボタン
- 更新情報を入力欄を表示するか

### 将来構想

- おすすめ表示


## 編集

### 概要

ホーム画面の編集ボタンから開く、作者専用の編集画面。

編集画面は、作者用Homeとは別のページとして扱う。

ログインした本人が、自分のRoomを編集するために使用する。

### 決定事項

- 背景を変更できる
- アセットを追加、編集できる
- リンクを編集できる
- テーマを変更できる


### 編集フロー

Roomfolioでは、次の流れで部屋を編集します。

1. 背景を選択する

2. アセットを追加する
   - 0～8個まで追加できる

3. アセットの種類を選択する
   - Frame（フレームタイプのアセット）
   - Object（家具のアセット）
   - オリジナル画像

4. アセットを設定する

5. 配置位置（1～8）を選択する
   - 保存データの配置位置は上書きできる
   - 上書き時は確認を表示する

6. 必要に応じてアセットを追加する

7. 保存する

RoomデータはSupabase側に保存し、画像はSupabase Storage、RoomfolioはURLを管理する。





### Roomの保存・更新

#### 初回保存時

- 新規作成したRoomは、新しいRoomとして保存する
- 保存されたRoomには固有のRoom IDを持たせる

#### 編集時

- 保存済みRoomを開くと、保存されているRoomデータを読み込む
- 背景、アセット、配置、アセット設定、リンク、テーマを復元する
- 保存済みのRoomを編集した場合は、同じRoom IDのデータを更新する
- 更新時に新しいRoomを作成しない
- 背景・アセット・リンクなど、現在のRoomの状態を更新する
- 削除されたアセット・リンクは、保存時にRoomのデータからも削除する
- キャンセルした場合は保存せず、元のRoomデータを変更しない


#### 編集時の配置位置

- 保存済みアセットを更新する場合、使用中の配置位置も選択できる
- 使用中の配置位置を選択した場合は、上書き確認を表示する
- 確認後、既存アセットを新しいアセットで置き換える


#### 保存データについて

- Roomの保存データはRoom IDを基準に管理する
- アセットはRoom IDに紐付ける
- リンクはアセットIDに紐付ける


---

### アセットの種類


Roomfolioでは、アセットを次の3種類に分類する。

- Frame：表示画像を設定できるフレーム型アセット
- Object：家具そのものを配置するアセット
- オリジナル画像：ユーザーがアップロードした画像を配置するアセット

#### Frame

表示画像を設定できるフレーム型アセット。

**設定内容**

- Frameを選択する
- 表示画像をアップロードする
- タイトルを設定する
- 説明を設定する
- リンクを設定する

#### Object

家具そのものを配置するアセット。

**設定内容**

- Objectを選択する
- タイトルを設定する
- 説明を設定する
- リンクを設定する

#### オリジナル画像

ユーザーが用意した画像をアップロードし、アセットとして配置する。

背景透過画像を利用することで、独自の家具や装飾として利用できる。


**設定内容**

- 表示画像をアップロードする
- タイトルを設定する
- 説明を設定する
- リンクを設定する




### 検討中

なし

### 将来構想

- 編集履歴


## エラー画面

### 概要

Roomfolioの世界観に合わせたエラー画面を表示する。

### 検討中

- 部屋が見つからない（404）
- 非公開の部屋
- 改装中
- サービスメンテナンス




# 未決定事項

## プロフィール

- 部屋内に配置するか、補足ページとして表示するか
- 表示位置
- UI・デザイン

## ホーム

- ボタン配置
- ナビゲーション方法
- 更新情報入力欄を表示するか
- おすすめ表示を行うか

## 検索

- 検索結果画面のレイアウト
- 並び順
- おすすめとの組み合わせ
- 検索結果の表示件数

## アセット

- アニメーション

## 部屋

- デフォルト背景のデザイン
- 背景プリセットの種類


## フォロー

- フォロー一覧の表示方法
- フォロワー一覧の表示方法
- 公開・非公開設定のUI

## 検索・発見

- おすすめユーザーの表示条件
- 人気順・新着順を用意するか

## 制限

- 画像サイズ
- 画像容量
- 対応画像形式
- リンク数の上限
- プロフィール文字数
- 更新情報の文字数

## その他

- 404ページ
- エラーメッセージの表示方法
- ローディング表示